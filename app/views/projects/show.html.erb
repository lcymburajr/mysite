<div class="container back-end">
  <div class="row">
    <div class="col-md-12">
      <h2><%= @project.title %></h2>
      <%= image_tag(@project.image.url(:thumb, timestamp:false)) %>
    </div>
  </div>
</div>